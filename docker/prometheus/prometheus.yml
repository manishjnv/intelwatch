global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Uncomment when api-gateway is deployed
  # - job_name: 'etip-api-gateway'
  #   static_configs:
  #     - targets: ['etip_api_gateway:3001']
  #   metrics_path: /metrics
