{
  "name": "@etip/shared-types",
  "version": "1.0.0",
  "description": "Zod schemas and TypeScript types for all ETIP entities",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "clean": "rimraf dist"
  },
  "dependencies": { "zod": "^3.23.0" },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@vitest/coverage-v8": "^2.0.0",
    "rimraf": "^6.0.0"
  },
  "files": ["dist"],
  "license": "UNLICENSED"
}
